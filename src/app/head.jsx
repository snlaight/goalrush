const Head = () => (
    <head>
      {/* favicon */}
      <link rel='icon' href='/favicon.ico' type='image/x-icon' />
      {/* <script async defer src={`https://maps.googleapis.com/maps/api/js?key=${process.env.NEXT_PUBLIC_MAPS_API_KEY}&callback=initMap`} /> */}
      <title>Goal Rush</title>
    </head>
  );

  export default Head;
